<h1>New Comentario</h1>

<%= form_for @comment, url: wiki_entry_comments_path(@entry), method: :post do |form|%>
<%= render 'form', form:form, comment: @comment, entry:@entry %>
<%end%>

<%= link_to 'Back', wiki_entry_path(@entry) %>
