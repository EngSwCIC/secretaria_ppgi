<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @announcement.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @announcement.body %>
</p>

<%= button_to 'Edit', edit_announcement_path(@announcement), :method => :get %> |
<%= button_to 'Back', announcements_path, :method => :get %>
