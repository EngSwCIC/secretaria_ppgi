<h2>Credenciamentos</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div class="actions">
    <%= f.submit "Log in" %>
  </div>
<% end %>

<%= render "credenciamentos/shared/links" %>
