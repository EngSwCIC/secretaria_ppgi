<p id="notice"><%= notice %></p>

<!--
<h1>Budgets</h1>

<table>
  <thead>
    <tr>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    < @budgets.each do |budget| %>
      <tr>
        <td><= budget.value %></td>
        <td><= link_to 'Show', budget %></td>
        <td><= link_to 'Edit', edit_budget_path(budget) %></td>
        <td><= link_to 'Destroy', budget, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    < end %>
  </tbody>
</table>

<br>
<= link_to 'New Budget', new_budget_path %>
-->

<p id="notice"><%= notice %></p>

<h1>Orçamento</h1>

<h1><%= @value %></h1>
<table>
  <thead>
  <tr>
    <th>Histórico</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @logs.each do |log| %>
    <tr>
      <td><%= log.value %></td>
      <td><%= button_to 'Editar Movimentação', edit_log_path(log), method: :get %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= button_to 'Nova Movimentação', new_log_path, method: :get %>

