<table>
    <thead>
        <tr>
        <th>Nome</th>
        <th>Matrícula</th>
        <th>Código do Processo</th>
        <th>Status</th>
        <th colspan="1"></th>
        </tr>
    </thead>

    <tbody>
        <% @sei_processes.each do |sei_process| %>
        <tr>
            <td><%= sei_process.user.full_name %></td>
            <td><%= sei_process.user.registration %></td>
            <td><%= sei_process.id %></td>
            <td><%= sei_process.status %></td>
            <% if sei_process.status != 'Espera' %>
            <td><%= link_to 'Mostrar', sei_process_path(sei_process) %></td>
            <% else %>
            <td><%= link_to 'Avaliar', edit_sei_process_path(sei_process) %></td>
            <% end %>
        </tr>
        <% end %>
    </tbody>
</table>
