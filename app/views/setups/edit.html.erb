<h1>Editar prazo</h1>

<%= render 'form', setup: @setup %>

<div style="display: flex; flex-direction: row">
  <%= button_to 'Voltar', setups_path, :method => :get %>
  <div style="margin-left: 50px"><%= button_to 'Mostrar', @setup, :method => :get %></div>
</div>